---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-view.fullname" . }}-configs
  labels:
    {{- include "nginx-view.labels" . | nindent 4 }}
data:
  {{-  (include "nginx-view.getConfigs" .) | nindent 2 }}
