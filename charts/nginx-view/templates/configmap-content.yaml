---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-view.fullname" . }}-contents
  labels:
    {{- include "nginx-view.labels" . | nindent 4 }}
data:
  {{-  (include "nginx-view.getContents" .) | nindent 2 }}
