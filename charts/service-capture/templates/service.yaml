apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-capture.fullname" . }}
  labels:
    {{- include "service-capture.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-capture.selectorLabels" . | nindent 4 }}
